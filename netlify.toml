[build]
  command = "npm run build"
  publish = "build"
  environment = { NODE_VERSION = "18", NODE_OPTIONS = "--openssl-legacy-provider" } # این خط برای رفع خطای OpenSSL اضافه شده است

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
